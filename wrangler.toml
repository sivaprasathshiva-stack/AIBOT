name = "ananya-telegram-bot"
main = "src/index.ts"
compatibility_date = "2026-04-20"

[vars]
BOT_NAME = "Ananya"
DEFAULT_TIMEZONE = "Asia/Kolkata"
FREE_DAILY_MESSAGE_LIMIT = "50"
GEMINI_MODEL = "gemini-2.5-flash-lite"
SUPABASE_URL = "https://xphwixmfafhgquivmqno.supabase.co"
# Set `APP_URL` to your deployed Worker URL (used by webhook script). Replace after deploy.
APP_URL = "https://<your-worker-domain>"
TELEGRAM_WEBHOOK_SECRET = "a1b2c3d4e5f60718293a4b5c6d7e8f90"
# NOTE: The following entries are convenient for local testing via .dev.vars or when using wrangler vars,
# but consider using `wrangler secret put` for sensitive values when deploying.
# Secrets are stored in Cloudflare secrets. Do not store them in this file.
